function partnerTitle(p,t){return ({'freed-shield':t('Help with lender harassment'),'lending-low-cost':t('Lower-cost loan pathway'),'payments-pickup-upi':t('UPI at pickup or delivery'),'send-bank-transfer':t('Send money home')})[p.partnerId]||t(p.name);}
// Central-owned member and partner views. No service is activated from the browser.
export function membershipMarkup(state,{t,esc,lang},recovery=false){
  const intro=`<p>${t('Your Nia team reviews membership and identity changes.')}</p>`;
  if(!state)return intro;
  const labels={ready:t('Membership approved'),wait_for_review:t('Awaiting identity review'),contact_jat_unit:t('Contact your Nia team'),recovery_pending:t('Phone change awaiting review'),enrol:t('Join Nia')};
  const status=`<div class="stack"><span class="badge">${esc(labels[state.next]||t('Contact your Nia team'))}</span>${state.memberId?`<p>${t('Member ID','सदस्य ID')}: <strong>${esc(state.memberId)}</strong></p>`:''}${state.member?.phoneMasked?`<p>${esc(state.member.phoneMasked)}</p>`:''}${intro}`;
  if((recovery||state.next==='enrol')&&!state.canSubmitIdentity)return status+`<p class="info">${t('Sign in with a verified phone before requesting enrolment or a number change. Your Nia team can help.')}</p></div>`;
  if(recovery)return status+`<form id="member-recovery-form" class="stack"><label>${t('Previous mobile number')}<input name="oldPhone" type="tel" inputmode="numeric" pattern="[6-9][0-9]{9}" maxlength="10" required></label><p>${t('Your verified sign-in supplies the new number. An operator must verify the change.')}</p><button class="primary">${t('Request help','मदद माँगें')}</button><p id="form-error" role="alert" class="error-inline"></p></form></div>`;
  if(state.next==='enrol')return status+`<form id="member-enrol-form" class="stack"><label>${t('Full name')}<input name="fullName" maxlength="120" minlength="2" autocomplete="name" required></label><input type="hidden" name="preferredLanguage" value="${esc(lang)}"><p>${t('Show your identity documents to your Nia team. Do not upload them here.')}</p><label class="consent-choice"><input name="consent" type="checkbox" required>${t('I agree to enrolment and identity checks by Nia.')}</label><button class="primary">${t('Submit for review')}</button><p id="form-error" role="alert" class="error-inline"></p></form></div>`;
  return status+'</div>';
}
export function partnerState(status,t){return ({held:t('Held by Nia · partner not connected'),requested:t('Requested · awaiting partner'),forwarded:t('Sent to partner'),withdrawn:t('Withdrawn'),declined:t('Declined'),closed:t('Closed')})[status]||t('Contact your Nia team');}
export function partnerListMarkup(partners,referrals,{t,esc},kind){
  const list=partners.filter(p=>!kind||p.kind===kind);
  return `<div class="stack"><p>${t('Requests and updates are held in your Nia account.')}</p>${list.map(p=>{
    const own=referrals.filter(r=>r.partnerId===p.partnerId),open=own.find(r=>['held','requested','forwarded'].includes(r.status));
    return `<article class="panel stack"><div class="row"><h3>${esc(partnerTitle(p,t))}</h3><span class="badge">${p.live?t('Accepting requests'):t('Not available yet','अभी उपलब्ध नहीं')}</span></div>${p.provider?`<p>${esc(p.provider)}</p>`:''}${p.requiresKyc?`<p class="muted">${t('Approved membership is required for financial services.')}</p>`:''}<p>${t('This is a referral request, not a policy, loan approval or payment.')}</p>${own.map(r=>`<div class="receipt"><strong>${esc(partnerState(r.status,t))}</strong><p>${t('Your reference','आपका संदर्भ')}: ${esc(r.referralId)}</p>${['held','requested','forwarded'].includes(r.status)?`<button data-action="partner-withdraw" data-id="${esc(r.referralId)}">${t('Withdraw request')}</button>`:''}</div>`).join('')}${!open&&p.acceptsReferrals&&!['payments','remittance'].includes(p.kind)?`<button data-action="partner-consent" data-id="${esc(p.partnerId)}">${t('Review request')}</button>`:''}</article>`;
  }).join('')||`<p>${t('Not available yet','अभी उपलब्ध नहीं')}</p>`}</div>`;
}
export function partnerConsentMarkup(p,{t,esc}){
  const labels={full_name:t('Full name'),phone:t('Mobile number','मोबाइल नंबर'),date_of_birth:t('Date of birth'),residence_studio:t('Your Nest','आपका नेस्ट'),employer:t('Employer'),niabooks_income_months:t('Monthly income records'),niabooks_expense_months:t('Monthly expense records'),nia_health_score:t('Nia health'),lender_name:t('Lender name'),incident_summary:t('Incident summary')};
  // Unknown consent fields require a reviewed member label before this UI can request them.
  const fields=p.consentScope.filter(key=>labels[key]);
  return `<form id="partner-referral-form" class="stack"><input type="hidden" name="partnerId" value="${esc(p.partnerId)}"><input type="hidden" name="consentVersion" value="${p.consentVersion}"><h3>${esc(partnerTitle(p,t))}</h3><p>${p.live?t('Your request will await the partner response.'):t('Nia will hold this request until the partner connection is ready.')}</p><fieldset class="stack"><legend>${t('Choose what you agree to share')}</legend>${fields.map(key=>`<label class="consent-choice"><input type="checkbox" name="fields" value="${esc(key)}">${esc(labels[key])}</label>`).join('')}</fieldset><label class="consent-choice"><input type="checkbox" name="consent" required>${t('I agree to this request and sharing only the records selected above with this partner.')}</label><p>${t('You can withdraw your request from this screen.')}</p><button class="primary" ${fields.length?'':'disabled'}>${t('Submit request')}</button><p id="form-error" role="alert" class="error-inline"></p></form>`;
}
