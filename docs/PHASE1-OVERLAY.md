# Phase 1 storefront overlay

This note is a write-access probe on `grok/ui-truth-recovery`.

The storefront wiring belongs in `commerce.js` plus `commerce-locales/{bn,ta,kn}.js`.
It must land immediately after this file if the authenticated push path works.
