# Central gate ledger

Append-only. The initial twelve blocks were created by Codex on 2026-09-13. No production gate evidence was supplied or captured in this task. Historical launch-log claims do not substitute for this ledger.

Codex may append PENDING and exact missing evidence. Only Sachin or the reviewing browser session may append PASS after both a captured success and a captured failure against production Central. Earlier blocks remain unchanged. No secret values or real-member data belong here.

## G1 signed member service boundary

```text
central SHA:      missing — verified production deployment SHA
request:          missing — captured signed request, key redacted, synthetic subject
response:         missing — captured production success for that subject
failure captured: missing — captured production refusal or boundary failure
verified by:      nobody; no production verification performed
status:           PENDING
missing:          Production signed success; nonce replay rejection; session-subject resolution; agreed error envelope and rate limit.
```

## G2 member session and access

```text
central SHA:      missing — verified production deployment SHA
request:          missing — captured signed request, key redacted, synthetic subject
response:         missing — captured production success for that subject
failure captured: missing — captured production refusal or boundary failure
verified by:      nobody; no production verification performed
status:           PENDING
missing:          Production enrolment/passkey/session success; recovery path; revocation within one cache window.
```

## G3 member.profile

```text
central SHA:      missing — verified production deployment SHA
request:          missing — captured signed request, key redacted, synthetic subject
response:         missing — captured production success for that subject
failure captured: missing — captured production refusal or boundary failure
verified by:      nobody; no production verification performed
status:           PENDING
missing:          Production language, residence, membership/access state and asOf; captured refused or unavailable result.
```

## G4 live.projection

```text
central SHA:      missing — verified production deployment SHA
request:          missing — captured signed request, key redacted, synthetic subject
response:         missing — captured production success for that subject
failure captured: missing — captured production refusal or boundary failure
verified by:      nobody; no production verification performed
status:           PENDING
missing:          Production Nest, Studio, fee position, service requests and source health; captured failure.
```

## G5 earn.projection

```text
central SHA:      missing — verified production deployment SHA
request:          missing — captured signed request, key redacted, synthetic subject
response:         missing — captured production success for that subject
failure captured: missing — captured production refusal or boundary failure
verified by:      nobody; no production verification performed
status:           PENDING
missing:          Production Studio/workplace pins, publication status/revision and complete commute provenance/expiry; captured failure.
```

## G6 save.projection

```text
central SHA:      missing — verified production deployment SHA
request:          missing — captured signed request, key redacted, synthetic subject
response:         missing — captured production success for that subject
failure captured: missing — captured production refusal or boundary failure
verified by:      nobody; no production verification performed
status:           PENDING
missing:          Production catalogue, price, stock, serviceability and PIN coverage; reviewed N0 evidence and captured failure.
```

## G7 claim.write

```text
central SHA:      missing — verified production deployment SHA
request:          missing — captured signed request, key redacted, synthetic subject
response:         missing — captured production success for that subject
failure captured: missing — captured production refusal or boundary failure
verified by:      nobody; no production verification performed
status:           PENDING
missing:          Production idempotency/revision success; 409 with current revision; concurrent replay proof and failure.
```

## G8 claim.status

```text
central SHA:      missing — verified production deployment SHA
request:          missing — captured signed request, key redacted, synthetic subject
response:         missing — captured production success for that subject
failure captured: missing — captured production refusal or boundary failure
verified by:      nobody; no production verification performed
status:           PENDING
missing:          Production agreed claim states, operator owner and next step; captured failure.
```

## G9 books.plan

```text
central SHA:      missing — verified production deployment SHA
request:          missing — captured signed request, key redacted, synthetic subject
response:         missing — captured production success for that subject
failure captured: missing — captured production refusal or boundary failure
verified by:      nobody; no production verification performed
status:           PENDING
missing:          Production dated entries and one versioned calendar-month plan in integer paise; coverage/correction/deduplication proof and failure.
```

## G10 Nia health

```text
central SHA:      missing — verified production deployment SHA
request:          missing — captured signed request, key redacted, synthetic subject
response:         missing — captured production success for that subject
failure captured: missing — captured production refusal or boundary failure
verified by:      nobody; no production verification performed
status:           PENDING
missing:          Production Central calculation version, completeness, consent and experimental label; failure; no lending coupling.
```

## G11 support cases

```text
central SHA:      missing — verified production deployment SHA
request:          missing — captured signed request, key redacted, synthetic subject
response:         missing — captured production success for that subject
failure captured: missing — captured production refusal or boundary failure
verified by:      nobody; no production verification performed
status:           PENDING
missing:          Production permission bootstrap, persisted case/owner, disabled-partner received path, authorised partner lifecycle and failure.
```

## G12 freshness and degraded states

```text
central SHA:      missing — verified production deployment SHA
request:          missing — captured signed request, key redacted, synthetic subject
response:         missing — captured production success for that subject
failure captured: missing — captured production refusal or boundary failure
verified by:      nobody; no production verification performed
status:           PENDING
missing:          Production asOf/source.status evidence and refusal/degraded behaviour for every screen; captured boundary failure.
```
